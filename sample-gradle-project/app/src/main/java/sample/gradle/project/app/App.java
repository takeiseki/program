/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package sample.gradle.project.app;

import sample.gradle.project.list.LinkedList;

import static sample.gradle.project.utilities.StringUtils.join;
import static sample.gradle.project.utilities.StringUtils.split;
import static sample.gradle.project.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
